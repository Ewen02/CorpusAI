# Do not auto-install optional peer dependencies. better-auth declares `next` as an
# optional peer that pnpm would otherwise pull into apps/api's dependency graph,
# dragging Next.js (~300MB) into the API Docker image. The API has no use for it.
auto-install-peers=false
