Build the project and report any errors.

Steps:

1. Run: `pnpm build`
2. If there are TypeScript errors, list them grouped by package
3. Suggest fixes for any type errors found
4. Report build success or failure

If a specific package is given as argument ($ARGUMENTS), build only that:
`pnpm --filter $ARGUMENTS build`
