[build]
dockerfilePath = "apps/web/Dockerfile"
watchPatterns = ["apps/web/**", "packages/ui/**", "packages/types/**", "packages/subscription/**"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 15
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
